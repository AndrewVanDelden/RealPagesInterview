# Build output
bin/
obj/
out/

# IDE
.vs/
.vscode/
.idea/
*.user
*.suo

# OS
.DS_Store
Thumbs.db

# Test run output
test-output.txt
tests/**/coverage/
mutation-output.txt
StrykerOutput/

# Secrets - user-secrets live outside the repo by design, these are defense in depth
*.env
.env
secrets.json
appsettings.*.local.json

# Personal agent instructions
CLAUDE.local.md

# Run artifacts written by the CLI at the repo root (--output, --replay input,
# --diagnostics, --review-queue, --eval-report, --log-file). The documented runs
# in docs/OPERATIONS.md write these; none of them belongs in the repo.
/out*.json
/diag*.json
/q_*.json
/eval*.txt
/*.log
