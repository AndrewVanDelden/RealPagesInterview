@AGENTS.md

## Claude Code

- Run `.\test.ps1` after every implementation change and read `test-output.txt` before reporting.
- The user runs `dotnet user-secrets` commands. Propose them; never execute or echo the key.
