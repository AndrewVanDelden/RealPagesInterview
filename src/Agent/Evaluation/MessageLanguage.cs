namespace Agent.Evaluation;

// The languages the labeled sets contain. A stated language outside this list is
// reported as not measured, never guessed.
public enum MessageLanguage
{
    English,
    Spanish,
}
